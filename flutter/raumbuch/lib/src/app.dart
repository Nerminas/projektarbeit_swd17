import 'package:flutter/material.dart';

class App extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      title: 'Smartlife Raumbuch',
      home: Text('Test'),
    );
  }
}
